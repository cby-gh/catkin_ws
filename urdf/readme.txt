All the cartpolex.urdf files is come from the cartpole.xacro and cartpole_underactuated.urdf.
cartpole.xacro represents rules of gazebo and ROS, cartpole_underactuated.urdf represents rules of Drake.
each cartpolex.urdf should changes name as cartpole2.urdf when used beacuse of launch files.

cartpole1.urdf is for version without controllers == Version 1 in fig folder.
cartpole2.urdf is for version w/ PID controllers == Version 2 in fig folder.
